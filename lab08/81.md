# Lab 8.1 - Wprowadzone zmiany (`DCF.ipynb`)

Poniżej znajduje się komplet zmian wykonanych w ćwiczeniu 8.1 (filtry korelacyjne) wraz z fragmentami kodu i krótkim uzasadnieniem.

## 1. Przejście z Colab na tryb lokalny (VS Code/Jupyter)

### Co zostało zmienione
- Usunięto zależność od `google.colab` i `drive.mount(...)`.
- Dodano automatyczne wykrywanie katalogu ćwiczenia oraz ustawianie `DATASET_DIR` lokalnie.
- Dodano lokalną wersję `cv2_imshow` opartą o `matplotlib`.

### Fragment kodu
```python
from pathlib import Path
import matplotlib.pyplot as plt

NOTEBOOK_DIR = Path.cwd()
if (NOTEBOOK_DIR / "sequences").exists():
    EXERCISE_DIR = NOTEBOOK_DIR
elif (NOTEBOOK_DIR / "lab08" / "sequences").exists():
    EXERCISE_DIR = NOTEBOOK_DIR / "lab08"
else:
    raise FileNotFoundError("Cannot locate lab08/sequences directory from current working directory.")

DATASET_DIR = str(EXERCISE_DIR / "sequences")


def cv2_imshow(img):
    if img.ndim == 3:
        display_img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
    else:
        display_img = img
    plt.figure(figsize=(6, 4))
    plt.imshow(display_img, cmap=None if img.ndim == 3 else "gray")
    plt.axis("off")
    plt.show()
```

## 2. Bezpieczne wczytywanie obrazów (zgodność z Pylance)

### Co zostało zmienione
- Dodano funkcję `read_image_checked`, która sprawdza, czy `cv2.imread(...)` zwróciło obraz.
- Zastąpiono bezpośrednie `cv2.imread(...)` w miejscach użycia funkcją bezpieczną.

### Fragment kodu
```python
from typing import cast


def read_image_checked(image_path):
    image = cv2.imread(image_path)
    if image is None:
        raise FileNotFoundError(f"Unable to read image: {image_path}")
    return cast(np.ndarray, image)
```

## 3. TODO (1) i TODO (2): `crop_search_window(...)`

### Co zostało zmienione
- Okno wyszukiwania jest tworzone wokół środka obiektu i skalowane przez `SEARCH_REGION_SCALE`.
- Dodano obsługę wyjścia poza granice obrazu przez dopełnienie (`cv2.copyMakeBorder`, `BORDER_REFLECT`).
- Zapewniono stały rozmiar okna (zgodny z oczekiwaniami FFT i mapy Gaussa), co usuwa błąd niedopasowania wymiarów.
- Na końcu konwersja do skali szarości.

### Fragment kodu
```python
def crop_search_window(bbox, frame):
    xmin, ymin, width, height = bbox
    center_x = xmin + (width / 2.0)
    center_y = ymin + (height / 2.0)

    search_width = max(2, int(round(width * SEARCH_REGION_SCALE)))
    search_height = max(2, int(round(height * SEARCH_REGION_SCALE)))

    xmin = int(round(center_x - (search_width / 2.0)))
    ymin = int(round(center_y - (search_height / 2.0)))
    xmax = xmin + search_width
    ymax = ymin + search_height

    frame_height, frame_width = frame.shape[:2]
    pad_left = max(0, -xmin)
    pad_top = max(0, -ymin)
    pad_right = max(0, xmax - frame_width)
    pad_bottom = max(0, ymax - frame_height)

    if any(pad > 0 for pad in [pad_left, pad_top, pad_right, pad_bottom]):
        frame = cv2.copyMakeBorder(
            src=frame,
            top=pad_top,
            bottom=pad_bottom,
            left=pad_left,
            right=pad_right,
            borderType=cv2.BORDER_REFLECT,
        )
        xmin += pad_left
        xmax += pad_left
        ymin += pad_top
        ymax += pad_top

    window = frame[ymin:ymax, xmin:xmax, :]
    if window.shape[0] != search_height or window.shape[1] != search_width:
        window = cv2.resize(window, dsize=(search_width, search_height), interpolation=cv2.INTER_LINEAR)

    window = cv2.cvtColor(window, cv2.COLOR_BGR2GRAY)
    return window
```

## 4. TODO (3): `pre_process(...)`

### Co zostało zmienione
- Dodano normalizację intensywności przez logarytm (`log(img + 1)`).
- Dodano standaryzację (zero-mean, unit-variance z małym `epsilon`).
- Pozostawiono okno Hanninga 2D.

### Fragment kodu
```python
def pre_process(img):
    height, width = img.shape
    img = img.astype(np.float32)

    img = np.log(img + 1.0)
    img = (img - np.mean(img)) / (np.std(img) + 1e-5)

    win_col = np.hanning(width)
    win_row = np.hanning(height)
    mask_col, mask_row = np.meshgrid(win_col, win_row)
    window = mask_col * mask_row
    img = img * window

    return img
```

## 5. TODO (4): `random_warp(...)`

### Co zostało zmienione
- Zaimplementowano losową augmentację przez obrót i skalę.
- Dodano parametry do ręcznego strojenia: `max_rotation`, `scale_range`.
- Użyto `BORDER_REFLECT`, aby ograniczyć artefakty na brzegach.

### Fragment kodu
```python
def random_warp(img, max_rotation=10.0, scale_range=(0.9, 1.1), rng=None):
    if rng is None:
        rng = np.random.default_rng()

    height, width = img.shape
    center = (width / 2.0, height / 2.0)
    rotation = rng.uniform(low=-max_rotation, high=max_rotation)
    scale = rng.uniform(low=scale_range[0], high=scale_range[1])

    matrix = cv2.getRotationMatrix2D(center=center, angle=rotation, scale=scale)
    img_warped = cv2.warpAffine(
        src=img,
        M=matrix,
        dsize=(width, height),
        flags=cv2.INTER_LINEAR,
        borderMode=cv2.BORDER_REFLECT,
    )
    img_resized = cv2.resize(img_warped, dsize=(width, height), interpolation=cv2.INTER_LINEAR)

    return img_resized
```

## 6. TODO (5): `predict(...)` i `update(...)`

### Co zostało zmienione
- `predict(...)`: wyliczanie odpowiedzi korelacyjnej w dziedzinie Fouriera i powrót do przestrzeni (`ifft2`).
- `update(...)`: aktualizacja modeli `Ai`, `Bi` z użyciem `LR` (uczenie online).

### Fragment kodu
```python
def predict(frame, position, H):
    search_window = crop_search_window(bbox=position, frame=frame)
    fi = pre_process(img=search_window)
    Fi = np.fft.fft2(fi)

    Gi = H * Fi
    gi = np.real(np.fft.ifft2(Gi))
    return gi


def update(frame, position, Ai, Bi, G):
    search_window = crop_search_window(bbox=position, frame=frame)
    fi = pre_process(img=search_window)
    Fi = np.fft.fft2(fi)

    new_Ai = (1 - LR) * Ai + LR * (G * np.conjugate(Fi))
    new_Bi = (1 - LR) * Bi + LR * (Fi * np.conjugate(Fi))
    return new_Ai, new_Bi
```

## 7. TODO (6): `update_position(...)`

### Co zostało zmienione
- Wyznaczanie maksimum mapy odpowiedzi (`argmax`).
- Przeliczenie przesunięcia względem środka mapy i aktualizacja pozycji obiektu (`x`, `y`).

### Fragment kodu
```python
def update_position(spatial_response, position):
    peak_y, peak_x = np.unravel_index(np.argmax(spatial_response), spatial_response.shape)
    response_height, response_width = spatial_response.shape

    delta_x = peak_x - (response_width // 2)
    delta_y = peak_y - (response_height // 2)

    if delta_x > response_width / 2:
        delta_x -= response_width
    if delta_y > response_height / 2:
        delta_y -= response_height

    new_position = position.copy()
    new_position[0] = float(new_position[0] + delta_x)
    new_position[1] = float(new_position[1] + delta_y)
    return new_position
```

## 8. Dodatkowe poprawki jakości kodu

### Co zostało zmienione
- Zmieniono część wywołań na argumenty nazwane (keyword arguments), zgodnie z wytycznymi.
- Usunięto nieużywany import `imutils` (ostrzeżenie Ruff).

### Fragment kodu
```python
response = predict(frame=image, position=position, H=Ai / Bi)
new_position = update_position(spatial_response=response, position=position)
newAi, newBi = update(frame=image, position=new_position, Ai=Ai, Bi=Bi, G=G)
```

## 9. Weryfikacja po zmianach

### Statyczna analiza
- `ruff check`: **All checks passed**.
- `basedpyright` (Pylance): **0 errors, 0 warnings, 0 notes**.

### Test uruchomieniowy
- Sekwencja: `sunshade`.
- Wynik: `Mean IoU = 0.7888417563430223`.

To potwierdza, że implementacja działa end-to-end i została domknięta dla ćwiczenia 8.1.
